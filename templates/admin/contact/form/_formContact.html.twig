{{ form_start(form_contact) }}
    <div class="form-group">
        {{form_row(form_contact.title)}}
    </div>
    <div class="form-group">
        {{form_row(form_contact.adress)}}
    </div>
    <div class="form-group">
        {{form_row(form_contact.email)}}
    </div>
    <div class="form-group">
        {{form_row(form_contact.telephone)}}
    </div>
    <div class="form-group">
        {{form_row(form_contact.facebook)}}
    </div>
    <div class="form-group">
        {{form_row(form_contact.twitter)}}
    </div>
    <button type="submit" class="btn btn-block btn-success">Mettre a jour</button>

{{ form_end(form_contact) }}
